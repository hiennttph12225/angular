export interface signinType {
      email : string;
      password : string;
}
export interface User {
      email : string;
      password : string;
}
export interface localStoragetype{
      token : string;
      id : string ; 
      name : string;
      email:string;
      role : number
}